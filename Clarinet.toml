[project]
name = 'decentralized-pet-care-network'
description = ''
authors = []
telemetry = true
cache_dir = './.cache'
requirements = []
[contracts.emergency-response]
path = 'contracts/emergency-response.clar'
clarity_version = 3
epoch = 'latest'

[contracts.pet-care-matching]
path = 'contracts/pet-care-matching.clar'
clarity_version = 3
epoch = 'latest'
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false

[repl.remote_data]
enabled = false
api_url = 'https://api.hiro.so'
